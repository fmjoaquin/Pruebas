node {
    stage('Example') {       
        echo 'I only execute on the master branch'        
    }
    
     stage('Example2') {       
        echo 'I only execute on the master branch'
    }
    
    stage('Example3') {       
        echo 'I only execute on the master branch'
    }
}
