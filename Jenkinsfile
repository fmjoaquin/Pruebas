node {
    stage('Example') {       
        for (i in [ 'a', 'b', 'c' ]) {
            echo i
         }
    }
    
     stage('Example2') {       
        echo 'I only execute on the master branch'
    }
    
    stage('Example3') {       
        echo 'I only execute on the master branch'
    }
}
